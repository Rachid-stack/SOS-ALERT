import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'geo-locate-v3',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
